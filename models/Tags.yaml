type: array
description: |-
  Add up to 10 comma-separated tags.

  NOTE:  If patching the tag array items you must pass the full array.  You cannot add, edit or remove individual items from the array.
uniqueItems: true
maxItems: 10
examples:
  - - tag1
    - tag2
    - tag3
items:
  type: string
